import React from 'react';
import logo from './../images/logo.png';

const CartWidget = ()=>{
  return <img className="materialboxed" width="100" height="60" src={logo} />;
}

export default CartWidget;
