import React from 'react';
import { Row, Col, Section } from 'react-materialize';

const ItemListContainer = ({msgInicio})=>{
    return <Section className="center">
                <h4>{msgInicio}</h4>
          </Section>
}

export default ItemListContainer;
